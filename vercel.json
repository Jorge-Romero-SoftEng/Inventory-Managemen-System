{
  "buildCommand": "echo postinstall && npm run postinstall && echo dbpush && npm run db:push:vercel && echo dbseed && npm run db:seed && echo buildfinished && npm run build",
  "installCommand": "echo installci && npm run install:ci"
}
